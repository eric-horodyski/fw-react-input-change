import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fw-react-input-change',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
